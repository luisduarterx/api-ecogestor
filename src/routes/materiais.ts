import { Request, Response, Router } from "express";

export const materiaisRoutes = Router();

// //Lida com o Materiais
// materiaisRoutes.get("/materiais", async (req:Request,res:Response)=>{

// })
// materiaisRoutes.get("/materiais/busca", async (req:Request,res:Response)=>{

// })

// materiaisRoutes.post("/materiais", async (req:Request,res:Response)=>{

// })

// materiaisRoutes.put("/materiais/:id", async (req:Request,res:Response)=>{

// })

// materiaisRoutes.delete("/materiais/:id", async (req:Request,res:Response)=>{

// })
