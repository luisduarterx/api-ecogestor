import { Request, Response, Router } from "express";

export const usersRoutes = Router();

// usersRouts.get("/usuarios", async (req:Request,res:Response)=>{

// })
// usersRouts.post("/usuarios", async (req:Request,res:Response)=>{

// })
// usersRouts.put("/usuarios/:id", async (req:Request,res:Response)=>{

// })

// usersRouts.delete("/usuarios/:id", async (req:Request,res:Response)=>{

// })
